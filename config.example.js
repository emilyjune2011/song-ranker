/**
 * Copy to config.js and fill in (config.js is gitignored):
 *   cp config.example.js config.js
 *
 * Spotify: https://developer.spotify.com/dashboard — Client ID (public; safe in client apps).
 * Supabase: run supabase-schema.sql, enable Anonymous auth, Settings → API for URL + anon key.
 */
window.SONG_RANKER_CLIENT_ID = "";

window.SONG_RANKER_SUPABASE_URL = "";
window.SONG_RANKER_SUPABASE_ANON_KEY = "";
