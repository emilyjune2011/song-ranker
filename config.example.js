/**
 * Optional local overrides (gitignored). Copy to config.js:
 *   cp config.example.js config.js
 *
 * Spotify Client ID is set in config.public.js (committed).
 * Use this file only for Supabase cloud sync, or to override the Client ID.
 */
window.SONG_RANKER_SUPABASE_URL = "";
window.SONG_RANKER_SUPABASE_ANON_KEY = "";
